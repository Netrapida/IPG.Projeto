<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="IPG.Projeto.Mobile.Views.NewItemPage"
		     Title="New Item">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Text" FontSize="Medium" />
            <Entry Text="{Binding Problem.Text}" FontSize="Small" />
            <Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding Problem.Detail}" FontSize="Small" Margin="0" />

            <Label Text="Lat:" FontSize="Medium" />
            <Label Text="{Binding Problem.Latitude}" FontSize="Small"/>
            <Label Text="Long:" FontSize="Medium" />
            <Label Text="{Binding Problem.Longitude}" FontSize="Small"/>

            <Label Text="Date:" FontSize="Medium" />
            <Label Text="{Binding Problem.ReportDate}" FontSize="Small"/>

            <Label Text="Council:" FontSize="Medium" />
            <Label Text="{Binding Problem.CouncilID}" FontSize="Small"/>
            <Label Text="{Binding Pin.Council_name}" FontSize="Small"/>


        </StackLayout>
    </ContentPage.Content>
</ContentPage>
